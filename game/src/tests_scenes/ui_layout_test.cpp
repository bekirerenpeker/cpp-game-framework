#include "test_funcs.hpp"

// Emptied while the UI system is rewritten by hand against UIManager/UINode. The
// previous scene targeted the deleted immediate-mode UiSystem; recover it from
// commit a886a32 if any of its 16 layout cases are worth porting.
int ui_layout_test() { return 0; }
